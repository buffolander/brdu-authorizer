/*

  async authorize(user: User, method: string, endpoint: string): Promise<boolean> {
    // query db 
    // match endpoint with routes
    // check if user is authorized to access
    return false
  }
  */